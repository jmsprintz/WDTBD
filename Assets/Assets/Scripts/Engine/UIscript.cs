using UnityEngine;
using System.Collections;
using UnityEngine.UI;

public class UIscript : MonoBehaviour {

    public Text health;
    public Text energy;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
        //health.text = "H: " + ShipController.health;
        //energy.text = "E: " + ShipController.energy_collected;
    }
}
